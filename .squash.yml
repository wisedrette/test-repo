deployments:
  pipelines:
    build_policy:
      allow_branches: config
    dockerimage: ubuntu:trusty
    pipelines:
      - website:
          build_policy:
            triggers: 
              - pr
          dockerimage: ubuntu:xenial
          build_steps:
            - apt-get update && apt-get install -y git curl
          scripts:
            - echo true
  app:
    dockerimage: nginx
    build_policy:
      allow_branches: config, build_policy
      triggers: 
        - commit
    pipelines:
      - website:
          scripts:
            - ls -la
      - test:
          dockerimage: ubuntu:latest
          build_steps:
            - which apt-get
          scripts:
            - true

